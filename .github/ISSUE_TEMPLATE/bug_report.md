---
name: Bug report
about: Something in the module doesn't work as documented
title: ""
labels: bug
---

**Describe the bug**
A clear description of what's wrong.

**Module version / ref**
e.g. `v1.0.0`, or a commit SHA if not using a tagged release.

**Terraform / provider versions**
`terraform version` output.

**Relevant configuration**
The `module "..." { ... }` block, with secrets replaced by placeholders.

**Steps to reproduce**
1.
2.

**Expected behavior**


**Actual behavior**
Include the exact error message or `terraform plan`/`apply` output if
relevant. For a runtime (Lambda) issue, include the relevant CloudWatch
Logs excerpt with any personal phone numbers/message content redacted.

**Additional context**
